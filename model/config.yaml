# config.yaml ─ glob pattern
# ──────────────────────────
input_csv: "data/interm/training_data.csv"

h5_paths_glob: "data/protspace/training_data.h5"

output_dir: "model/model_output/0800_all_model"

embedding_dim: 1024
hidden_dim:    128
batch_size:    64
num_epochs:    100
learning_rate: 0.0001
early_stopping_patience: 5